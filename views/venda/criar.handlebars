<div class="container-sm">
    <div class="table-responsive">
        <div class="table-wrapper" style="margin: auto;">
            <div class="table-title">
                <div class="row">
                    <div class="col-mb-3">
                        <h4>Nova Venda</h4>
                    </div>
                </div>
            </div>

            <form action="/venda/criarPost" method="POST">
                <div class="form-control">
                    <div class="mb-3">
                        <label for="data" class="form-label">Data</label>
                        <input type="date" class="form-control" name="data" value="{{data_atual}}" required>
                    </div>
                    <div class="mb-3">
                        <label for="data" class="form-label">Cliente</label>
                        <select class="form-select" aria-label="cliente" name="cliente" required>
                            <option selected>Selecione o cliente</option>
                            {{#each clientes}}
                            <option value="{{this.id}}">{{this.id}}. {{this.nome}}</option>
                            {{/each}}
                        </select>
                    </div>

                    {{!-- <form action="/cart" method="post" class="search">
                        <input class="form-control" type="text" placeholder="Nome do produto" name="nome" />
                        <button type="submit" class="btn btn-primary">Pesquisar</button>
                    </form> --}}
                    {{!-- <div class="mb-3 row">
                        <label for="valorTotal" class="form-label">Valor Total</label>
                        <div class="col-sm-10">
                            <input type="text" readonly class="form-control-plaintext" name="valorTotal" id="valortotal"
                                value="Um valor qualquer">
                        </div>
                    </div> --}}
                    {{!-- <div class="mb-3">
                        <label for="" class="form-label">Produtos da Venda</label>
                        <input type="text" class="form-control" name="valorTotal" value="" required>
                    </div> --}}

                    <center>
                        <button type="submit" class="btn btn-success"><i class="bi bi-check-circle-fill"></i> Iniciar Venda</button>
                        <a href="/venda" class="btn btn-danger"><i class="bi bi-x-circle-fill"></i> Cancelar</a>
                    </center>
                </div>
            </form>
        </div>
    </div>
</div>


<script>



</script>